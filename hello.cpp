#include <iostream>
using namespace std;

int main()
{
    cout << "Add a line\n";
    cout << "hello World\n";

    for
        i in range(len(numbers)) : diff = abs(average - numbers[i]) if diff > max_diff : max_diff = numbers[i]
}